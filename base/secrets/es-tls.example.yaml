# Template — do not apply directly. The real Secret is materialized by
# `make secrets` after `make tls-certs`. The PKCS12 keystore is mounted
# only into ES pods (transport + http TLS). Real values are .gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: es-tls
  namespace: logging
  labels:
    app.kubernetes.io/part-of: efk
type: Opaque
# data:
#   elastic-certificates.p12: <base64 of certs/elastic-certificates.p12>
#   ca.crt:                   <base64 of certs/ca.crt>
