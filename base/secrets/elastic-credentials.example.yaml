# Template — do not apply directly. The real Secret is materialized by
# `make secrets`, which generates a random password and the XPACK
# encryption key. Real values are .gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: elastic-credentials
  namespace: logging
  labels:
    app.kubernetes.io/part-of: efk
type: Opaque
stringData:
  password: REPLACE_ME_GENERATED_BY_MAKE_SECRETS
  xpack_encryptionkey: REPLACE_ME_32_CHAR_RANDOM_STRING
